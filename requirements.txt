numpy>=1.26
pandas>=2.2
matplotlib>=3.8
seaborn>=0.13
gymnasium>=0.29.1
stable-baselines3>=2.3.0
tensorboard>=2.16
# Install PyTorch separately (CPU or CUDA). Examples:
# CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
# CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Optional big-data integration for training script when using Hive:
pyspark>=3.5.0
pyarrow>=15.0.0
